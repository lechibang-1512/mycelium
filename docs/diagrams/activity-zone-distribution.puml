@startuml activity-zone-distribution
title Activity Diagram: Zone Distribution & Optimization

skinparam backgroundColor #f8fafc
skinparam activityBackgroundColor #dbeafe
skinparam activityBorderColor #2b6cb0
skinparam activityFontColor #0f172a

|Staff/Admin|
start
:Access Zone Distribution;
:Select Warehouse & Product;

|System|
:Load Zones & Current Distribution;
note right
  Parallel loading:
  • Zone details (name, type, capacity)
  • Product locations by zone
  • Distribution metrics
end note

:Calculate Metrics;
note right
  • Total quantity across zones
  • Zones in use
  • Distribution variance
  • Utilization percentages
end note

:Display Distribution Dashboard;

|Staff/Admin|
if (Select Action?) then (View/Analyze)
  |System|
  :Show Zone Map & Charts;
  note right
    • Visual zone map (color-coded)
    • Distribution table
    • Utilization heatmap
    • Recommendations
  end note
  stop

elseif (Optimize) then
  :Select Optimization Strategy;
  note right
    Strategies:
    • Even Distribution
    • Capacity-Based
    • Type-Based Priority
    • Turnover-Based
    • Custom Rules
  end note
  
  |System|
  :Calculate Optimized Distribution;
  :Generate Movement Plan;
  note right
    Plan includes:
    • Required moves (source → destination)
    • Estimated time & cost
    • Efficiency improvement
    • Movement sequence
  end note
  
  :Display Optimization Plan;
  
  |Staff/Admin|
  if (Approve?) then (yes)
    if (Execute Now?) then (yes)
      |System|
      :Begin Execution;
      
      repeat
        :Show Next Move Instruction;
        |Staff/Admin|
        :Perform Physical Move;
        :Scan QR Codes & Confirm;
        |System|
        :Update Inventory Locations;
        :Log Movement;
        :Update Progress;
      repeat while (More moves?)
      
      :Display Summary;
      stop
      
    else (Schedule)
      |System|
      :Create Scheduled Task;
      :Send Notification;
      stop
    endif
  else (no)
    :Modify Parameters;
    backward:Return to strategy;
  endif

elseif (Manual Transfer) then
  |Staff/Admin|
  :Specify Source & Destination;
  :Enter Quantity;
  
  |System|
  :Validate Transfer;
  
  if (Valid?) then (yes)
    :Execute Transfer;
    :Update Locations & Log;
    :Display Success;
  else (no)
    :Show Errors;
  endif
  stop

else (View Analytics)
  |System|
  :Generate Analytics;
  note right
    • Zone efficiency metrics
    • Turnover rates
    • Activity heatmap
    • Bottleneck identification
    • Historical trends
  end note
  :Display Analytics Dashboard;
  stop
endif

@enduml

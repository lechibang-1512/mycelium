require express, mysql2, path, body-parser, ejs, express-session,
to install, just type npm install + package-name, i.e. npm install body-parser
follow on-screen commands to audit packages as needed.
